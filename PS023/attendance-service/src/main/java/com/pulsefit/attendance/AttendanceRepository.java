package com.pulsefit.attendance; import java.time.*; import java.util.*; import org.springframework.data.jpa.repository.JpaRepository; public interface AttendanceRepository extends JpaRepository<Attendance,Long>{List<Attendance> findByMemberIdOrderByCheckInTimeDesc(Long memberId); boolean existsByMemberIdAndFacilityAndCheckInTimeBetween(Long memberId,String facility,LocalDateTime from,LocalDateTime to);}
